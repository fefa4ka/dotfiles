% signalq routines

# Make
make <test>

# Sign <lnx_rfs_devfw> by <private_pem>
openssl dgst -sha256 -sign <private_pem> <lnx_rfs_devfw> > <lnx_rfs_devfw>.sha256.signed 

$ private_pem: ls *.pem
# lnx_rfs_devfw: ls *.devfw
